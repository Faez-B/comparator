<nav class="p-2 relative">
    {% if not app.user %}
        <div class="text-right">
            <a href="{{ path('login') }}">
                <button class="
                bg-green-600 
                text-white 
                p-1 px-2  
                rounded-md
                border-2 border-green-600 
                hover:bg-white hover:text-green-600
                ">
                Se connecter
                </button>
            </a>

            <a href="{{ path('register') }}">
                <button class="
                bg-blue-600 
                text-white 
                p-1 px-2  
                rounded-md
                border-2 border-blue-600 
                hover:bg-white hover:text-blue-600
                ">
                S'enregistrer
                </button>
            </a>
        </div>

        {% for message in app.flashes('success_register') %}
            <div class="flash-message w-4/5 absolute bg-green-500 p-5 text-white top-0 left-2/4 -translate-x-1/2 flex justify-between items-center w-full">
                <div class="flash-notice">
                    {{ message }}
                </div>

                <span class="croix cursor-pointer">X</span>
            </div>
        {% endfor %}

    {% else %}
        <div class="text-right">
            <a href="{{ path('logout') }}">
                <button class="
                bg-red-600 
                text-white 
                p-1 px-2  
                border-2 border-red-600 
                rounded-md
                hover:bg-white hover:text-red-600
                ">
                Déconnexion
                </button>
            </a>
        </div>
    {% endif %}
</nav>