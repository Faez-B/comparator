<!DOCTYPE html>
<html lang="fr" class="h-full">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Comparator{% endblock %} - Comparaison intelligente</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🔍</text></svg>">
        
        {# Modern CSS with Tailwind #}
        <link href="{{ asset('css/output.css') }}" rel="stylesheet">
        
        {# Font Awesome for icons #}
        <script src="https://kit.fontawesome.com/ab6d801c6b.js" crossorigin="anonymous"></script>
        
        {% block stylesheets %}{% endblock %}
    </head>
    <body class="h-full bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100 antialiased">
        {# Modern Navbar #}
        {% include "navbar.html.twig" %}
        
        {# Flash Messages #}
        {% include "_flash_messages.html.twig" %}
        
        {# Main Content #}
        <main class="min-h-[calc(100vh-64px)]">
            {% block body %}{% endblock %}
        </main>
        
        {# Footer #}
        <footer class="bg-white dark:bg-gray-800 border-t border-gray-200 dark:border-gray-700 py-6 mt-12">
            <div class="page-container">
                <div class="text-center text-sm text-gray-600 dark:text-gray-400">
                    <p>&copy; {{ 'now'|date('Y') }} Comparator. Tous droits réservés.</p>
                    <p class="mt-2">Plateforme de comparaison intelligente pour véhicules et appareils.</p>
                </div>
            </div>
        </footer>
        
        {# Modern JavaScript #}
        <script src="{{ asset('js/main.js') }}" defer></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
