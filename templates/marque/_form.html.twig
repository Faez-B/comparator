{{ form_start(form) }}
    {{ form_widget(form.nom, { "attr" : { "autofocus" : true, "autocomplete" : true, "placeholder" : "Nom de la marque" } }) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
