{% extends 'base.html.twig' %}

{% block title %}Marques{% endblock %}

{% block body %}
<div class="page-container">
    <div class="flex items-center justify-between mb-4">
        <h1 class="text-3xl md:text-4xl font-bold mb-2 flex items-center">
            <i class="fa-solid fa-tag text-primary-600 mr-2"></i>
            Marques
        </h1>
        <a href="{{ path('marque_new') }}" class="btn btn-accent">
            <i class="fa-solid fa-plus mr-2"></i> Ajouter une marque
        </a>
    </div>
    <div class="card overflow-hidden p-0">
        <div class="overflow-x-auto">
            <table class="table-modern">
                <thead>
                    <tr>
                        <th>Nom</th>
                        <th class="w-36">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for marque in marques %}
                    <tr>
                        <td>
                            <span class="font-medium">{{ marque.nom }}</span>
                        </td>
                        <td>
                            <div class="flex space-x-2">
                                <a href="{{ path('marque_show', {'id': marque.id}) }}"
                                   class="btn btn-secondary btn-sm"
                                   title="Voir les détails"
                                   aria-label="Voir les détails de {{ marque.nom }}">
                                    <i class="fa-solid fa-eye mr-1"></i>
                                    Voir
                                </a>
                                <a href="{{ path('marque_edit', {'id': marque.id}) }}"
                                   class="btn btn-warning btn-sm"
                                   title="Modifier"
                                   aria-label="Modifier {{ marque.nom }}">
                                    <i class="fa-solid fa-pen mr-1"></i>
                                    Modifier
                                </a>
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2">Aucun résultat</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
