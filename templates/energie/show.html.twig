{% extends 'base.html.twig' %}

{% block title %}Energie{% endblock %}

{% block body %}
    <h1>Energie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ energie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ energie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_energie_index') }}">back to list</a>

    <a href="{{ path('app_energie_edit', {'id': energie.id}) }}">edit</a>

    {{ include('energie/_delete_form.html.twig') }}
{% endblock %}
